# 类型: 分流规则 — 系统联网检测直连（须在代理规则之前匹配）
# 仅含测网/Captive Portal；勿将 dns.google / 8.8.8.8 / 1.1.1.1 钉死 DIRECT，
# 否则国内解析走「谷歌/国外」上游时会直连超时（dns relay timeout）。

domain_set:
  - connectivitycheck.gstatic.com
  - www.msftconnecttest.com
  - captive.apple.com
