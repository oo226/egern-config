# Egern 懒人配置（guize）

```
https://raw.githubusercontent.com/oo226/egern-config/refs/heads/guize/Egern/Profile.yaml
```

- **外观短**：Profile 只挂少量规则集名字；广告分流表在 `Routing/`，模块在 `Yuanban/heji`  
- **去广告**：可莉为主，**不含**奶思 blockAds 整包（与可莉叠层易爆内存；main 日更是奶思主线）  
- **DNS**：无 `system` bootstrap；`hosts` 钉 DoH；不加 Loyalsoldier 直连大名单（减内存）

Sub-Store 组合订阅名填 `AllServer`。强制更新 Profile 后生效。
