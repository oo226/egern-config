# 签到脚本

镜像自 ZenmoFeiShi / chavyleung / Yuheng0101 等上游，由 Actions 每日同步。

在 `Egern.yaml` 的 `scriptings` 中引用，默认 **全部 disabled**，需要时手动开启并配置 Cookie/参数。

清单见 `scripts/manifest.yaml`（开发用路径，用户只需关心本目录的 `.js` 文件）。
